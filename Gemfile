source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gemspec

gem "rails", "6.0.0.rc1"
gem "devise"

group :test do
  gem "factory_bot"
  gem "simplecov"
  gem "codecov"
end
